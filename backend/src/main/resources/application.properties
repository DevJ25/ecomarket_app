spring.application.name=market

# Database Configuration
# Uses environment variables if available, otherwise defaults to local dev settings
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/EcoMarket_app}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:karjoel}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=always
spring.jpa.open-in-view=true

# JWT Configuration
jwt.secret.key=${JWT_SECRET:dGhpcy1pcy1hLXZlcnktc2VjdXJlLWtleS1mb3ItM256LWp3dC10b2tlbi1zaWduaW5nLXB1cnBvc2VzLW9ubHk=}
jwt.expiration=86400000

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=${MAIL_USERNAME:redc24990@gmail.com}
spring.mail.password=${MAIL_PASSWORD:lhgg glvr rrto efnl}
spring.mail.protocol=smtps
spring.mail.properties.mail.transport.protocol=smtps
spring.mail.properties.mail.smtps.auth=true
spring.mail.properties.mail.smtps.starttls.enable=true
spring.mail.properties.mail.smtps.timeout=8000
spring.mail.properties.mail.smtps.connectiontimeout=8000
spring.mail.properties.mail.smtps.ssl.enable=true
spring.mail.properties.mail.smtps.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.debug=true

# App Configuration
# Frontend URL for CORS and email links
app.frontend.url=${FRONTEND_URL:http://localhost:3000}